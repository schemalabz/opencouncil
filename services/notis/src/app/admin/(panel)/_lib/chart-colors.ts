/**
 * The two period colours every chart on the overview draws with. The trend
 * legend and the cards read the same values, so a key never drifts from
 * the marks it names.
 */
export const CURRENT_PERIOD = "#f97316";
export const PREVIOUS_PERIOD = "#c3c2b7";
