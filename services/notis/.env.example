# Anthropic API key used by the agent core (dry-run endpoint, playground).
ANTHROPIC_API_KEY=sk-ant-...

# Shared secret gating /admin and the agent API routes (min 16 chars).
# Replaced by shared-cookie auth in PR 2.
NOTIS_ADMIN_SECRET=dev-secret-dev-secret
# Optional: enables address search + map in the playground
NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=pk....
