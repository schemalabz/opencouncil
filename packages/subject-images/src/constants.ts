/**
 * The canonical image size, on its own so client components can import it
 * without dragging sharp and the S3 client into the browser bundle.
 * Wide because both surfaces that show it are wide.
 */
export const IMAGE_WIDTH = 1344;
export const IMAGE_HEIGHT = 768;
